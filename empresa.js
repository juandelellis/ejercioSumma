class Empresa{
    constructor(id, nombre, empleados) {
      this.id= id;
      this.nombre = nombre;
      this.empleados = empleados;
    }
  };

  const addEmpleado => (empleado) {
    empleados.push(empleado);
 }

console.log(`Sumar al nuevo empleado ${addEmpleado(Juan de Lellis)}`)


console.log(Empresa.values(empleados));

